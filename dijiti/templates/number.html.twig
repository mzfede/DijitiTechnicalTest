
<!DOCTYPE html>
<html>
  <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
  </head>
  <body>
    <h2>Benvenuti!</h2>
    <p>Qui puoi cercare, modificare o eliminare gli utenti iscritti.</p>
      <div id = "insertDiv">
        <button type="button" id="formButton" onclick="toggleForm()">Inserisci un nuovo utente</button>
        <form id = "insertForm" action="InserisciController.php" method = "post/get">
                <label>Nome:</label>
                <input type = "text" id = "nome" required>
                <label>Cognome:</label>
                <input type = "text" id = "cognome" required >
                <label>E-mail:</label>
                <input type = "email" id = "email" required>
                <label>Password:</label>
                <input type = "password" id = "password" required>
                <label>Telefono:</label>
                <input type = "text" id = "telefono" pattern="[0-9]{10}">
                <button type="submit" id="submit">Inserisci</button>
        </form>
      </div>
      <div id="deleteButtonDiv">
        <button type="button" id="deleteButton" onclick="deleteUser()">Elimina gli utenti selezionati</button>
      </div>
      <div id = "table">
        <table>
          <thead>
              <tr>
              {% for elem in tableHead %}
                  <th>{{ elem }}</th>
              {% endfor %}
              </tr>
          </thead>
          <tbody>
            {% for elem in sent %}
              <tr>
              {% for e in elem %}
                <td>{{ e }}</td>
              {% endfor %}
              <td><input type="checkbox" id="{{elem[3]}}"></td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>

<style>
  #insertForm{
    display:none;
  }
  body{
    padding: 3%;
  }
  button{
    padding: 5px;
    font-size: 0.8em;
    margin:5px;
  }

</style>
<script>
  function toggleForm() {
    var x = document.getElementById("insertForm");
    if (x.style.display === "none") {
      x.style.display = "block";
    } else {
      x.style.display = "none";
    }
  }
  function inserisciUtente(){

  }

  function deleteUser(){
    var aa= document.getElementsByTagName("input");
    for (var i =0; i < aa.length; i++){
        if (aa[i].type == 'checkbox')
            aa[i].checked = true;
    }
  }
</script>
